[package]
name = "sjlj2"
version = "0.1.0"
edition = "2021"

[features]
default = []
unstable-asm-goto = []

[dependencies]

[dev-dependencies]
criterion = "0.5.1"
pprof = { version = "0.14.0", default-features = false }

[[bench]]
name = "bench"
harness = false

[profile.release]
debug = "line-tables-only"
